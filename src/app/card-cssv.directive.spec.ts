import { CardCssvDirective } from './card-cssv.directive';

describe('CardCssvDirective', () => {
  it('should create an instance', () => {
    const directive = new CardCssvDirective();
    expect(directive).toBeTruthy();
  });
});
